interface:
  display_name: "Research GPU SSH"
  short_description: "Safely run experiments on a shared GPU server"
  default_prompt: "Use $research-gpu-ssh to inspect a shared GPU server and launch a reproducible experiment."
